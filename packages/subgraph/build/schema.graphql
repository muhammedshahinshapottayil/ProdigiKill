type Proposal @entity {
  id: ID!
  userAddress: Bytes!
  title: String!
  details: String!
  createdAt: BigInt!
  finalDate: BigInt!
  withdrawal: Boolean!
  status: Int!
  transactionHash: String!
}

type Sender @entity {
  id: ID!
  address: Bytes!
  # greetings: [ID!] @derivedFrom(field: "id")
  createdAt: BigInt!
  greetingCount: BigInt!
}
